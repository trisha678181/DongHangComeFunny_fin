<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ORDER">
		<insert id="insertOrder" >
			INSERT INTO tb_order(ouno, oimpuid, omerchantuid, oname, oqunatity, oprice, odate, oisdel)
			values(#{oUNo},#{oImpUid},#{oMerChantUid},#{oName},#{oQunatity},#{oPrice},#{oDate},0)
		
		</insert>
</mapper>